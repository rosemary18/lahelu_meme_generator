// IMPORT DEFINED GLOBAL TYPES

import { IObjectItem } from "../../types";

// DEFINE AND EXPORT THE PROPS INTERFACE

/* Define and export the ISavedEditMeme interface */
export interface ISavedEditMeme {
    items: IObjectItem[];
    backgroundColor: string;
}